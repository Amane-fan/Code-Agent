# OpenAI-compatible chat model 配置。
# 复制为 .env 后，把 API Key 占位值替换为真实 API Key。
# BASE_URL 可填 base URL；兼容完整 /chat/completions endpoint。

API_KEY=替换为你的 API Key
BASE_URL=https://api.deepseek.com
MODEL=deepseek-v4-flash
